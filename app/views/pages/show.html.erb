<p id="notice"><%= notice %></p>
<%= link_to '<i class="glyphicon glyphicon-pencil"></i>  Edit'.html_safe, edit_page_path(@page), class: 'btn btn-primary pull-right inverse' %>

<h2>
  <%= @page.name %>
</h2>

<p>
  <%= @page.about %>
</p>

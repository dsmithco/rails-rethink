<%= link_to '<i class="glyphicon glyphicon-pencil"></i>  Edit'.html_safe, edit_page_path(@page), class: 'btn btn-primary pull-right inverse' if can? :update, @page %>

<h2>
  <%= @page.name %>
</h2>

<p>
  <%= @page.about %>
</p>

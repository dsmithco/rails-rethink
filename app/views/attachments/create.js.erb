<% if @resource.new_record? %>
  alert("Failed to upload painting: <%= j @attachment.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#attachments").append("<%=j render 'attachments/attachment', attachment: @resource %>");
<% end %>

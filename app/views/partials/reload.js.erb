$("#<%= request.parameters['container'] %>")
  .html('<%=j render request.parameters['partial'], "#{request.parameters['model']}": request.parameters['id'].present? ? eval(request.parameters['model'].capitalize).find(request.parameters['id']) : eval(request.parameters['model'].capitalize).new(request.parameters['resource']) %>');

<% if @resource.new_record? %>
  alert("Failed to upload painting: <%= j @resource.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $(".hero_images").html("<%=j render 'hero_images/heros', hero_images: @resource.attachable.hero_images %>");
  $scope.$apply();
<% end %>
